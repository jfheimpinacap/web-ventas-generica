import { Layout } from '../components/layout/Layout'

export function LoginPage() {
  return (
    <Layout>
      <section className="simple-page">
        <h1>Login vendedor</h1>
        <p>Esta ruta queda preparada para implementar autenticación en una fase posterior.</p>
      </section>
    </Layout>
  )
}
